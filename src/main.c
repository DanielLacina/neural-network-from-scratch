#include <stdlib.h>
#include <stdio.h>
#include "array.h"
#include "multidim_array.h"



int main() {
    // multidim_array_print(identity_matrix(50));
    // Array arr1;
    // Array arr2; 
    // Array arr3; 
    // MultiDimensionalArray mda; 
    // multidim_array_init(&mda); 
    // float arr_data[] = {1, 2};
    // array_init(&arr1);
    // array_init(&arr2);
    // array_init(&arr3);
    // array_add_arr(&arr1, arr_data, 2);
    // array_add_arr(&arr2, arr_data, 2);
    // array_add_arr(&arr3, arr_data, 2);
    // multidim_array_add(&mda, &arr1); 
    // multidim_array_add(&mda, &arr2); 
    // multidim_array_add(&mda, &arr3); 
    // MultiDimensionalArray *transposed_mda = transpose_multidim_array(&mda);
    // multidim_array_print(transposed_mda);
    // multidim_array_print(&mda);
    // multidim_array_print(multiply_arrays_as_matrix(transposed_mda, &mda));
    // multidim_array_add(&mda, &arr1); 
    // multidim_array_add(&mda, &arr2); 
    // multidim_array_print(&mda);
    // multidim_array_print(transpose_multidim_array(&mda));
    // multidim_array_print(multiply_two_multidimensional_arrays(&mda, &mda)); // Uncomment this to print result
}
